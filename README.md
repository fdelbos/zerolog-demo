this is a demo of [zerolog](https://github.com/rs/zerolog)

- build: `go build .`
- run text format `FORMAT=text ./zerolog-demo`
- run text format, but only errors `LEVEL=error FORMAT=text ./zerolog-demo`
- run json format `./zerolog-demo`